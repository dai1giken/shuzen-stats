__pycache__/
*.pyc
raw_estat.json
*.pdf

# 内部向け。立ち位置の分析や事故の記録が入っているため公開しない。
README_内部.md

# 社内PC用のローカル実行ラッパー。OneDrive配下の運用基盤を参照するので公開しない。
# 自動更新は .github/workflows/update.yml が担う。
run_update.ps1

# 企業サイト用に生成する技術コラム。**公開先が違う**（dai1giken.co.jp/column/）ので
# statsサイトのデプロイには混ぜない。生成器 build_column.py はリポジトリに置く。
column/

# ホスト直下（htdocs/）用の robots.txt と sitemap.xml。
# statsサイトのデプロイには混ぜない（公開先が違う）。
_root/

# 手元で作るアップロード用 ZIP（make_upload_zip.py）。
# 通常は GitHub Actions が Release に添付するので、リポジトリには置かない。
shuzen-stats.zip
